production:
    rails:
        server:
            uniquename: frontend
        configuration:
                nginx_cors:
                origin: '*'
                methods: 'GET, OPTION'
                mime_regex: '\\.(?:ttf|ttc|otf|eot|woff|font.css)$'
